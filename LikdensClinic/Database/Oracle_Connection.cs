//using Oracle.DataAccess.Client;

namespace LikdensClinic.Database
{
    class Oracle_Connection
    {
        /*public static OracleConnection GetDBConnection(string host, int port, String sid, String user, String password)
        {
            string connString = "Data Source=(DESCRIPTION =(ADDRESS = (PROTOCOL = TCP)(HOST = "
                 + host + ")(PORT = " + port + "))(CONNECT_DATA = (SERVER = DEDICATED)(SERVICE_NAME = "
                 + sid + ")));Password=" + password + ";User ID=" + user;


            OracleConnection conn = new OracleConnection();

            conn.ConnectionString = connString;

            return conn;
        }
        public static OracleConnection GetDBConnection()
        {
            string host = "localhost";//"192.168.205.1";
            int port = 1521;
            string sid = "Likdens";
            string user = "likdens";
            string password = "likdens123";

            return GetDBConnection(host, port, sid, user, password);
        }*/
    }
}
